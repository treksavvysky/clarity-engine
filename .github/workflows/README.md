# GitHub Workflows

Workflow definitions live here. The Stage 0 CI pipeline installs Python 3.12, runs unit tests, lints the example manifest, and composes a packet from it.

Add or adjust workflows when new tooling or stages introduce additional checks; keep jobs network-free and deterministic.
